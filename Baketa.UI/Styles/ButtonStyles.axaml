<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- 🔥 [ISSUE#164] UI/UX改善用のボタンスタイル -->

    <!-- カラーリソース定義 -->
    <!-- 🔥 [ISSUE#164_FIX] 背景を透明に、ホバー時のみ薄いグレー表示 -->
    <Styles.Resources>
        <!-- Default State Colors -->
        <SolidColorBrush x:Key="ButtonDefaultBackground">Transparent</SolidColorBrush>
        <SolidColorBrush x:Key="ButtonDefaultForeground">#FFFFFF</SolidColorBrush>
        <SolidColorBrush x:Key="ButtonDefaultBorder">Transparent</SolidColorBrush>

        <!-- Hover State Colors -->
        <SolidColorBrush x:Key="ButtonHoverBackground">#30FFFFFF</SolidColorBrush>
        <SolidColorBrush x:Key="ButtonHoverForeground">#FFFFFF</SolidColorBrush>
        <SolidColorBrush x:Key="ButtonHoverBorder">#40FFFFFF</SolidColorBrush>

        <!-- Active State Colors -->
        <SolidColorBrush x:Key="ButtonActiveBackground">#40FF4444</SolidColorBrush>
        <SolidColorBrush x:Key="ButtonActiveForeground">#FFFFFF</SolidColorBrush>
        <SolidColorBrush x:Key="ButtonActiveBorder">#FF4444</SolidColorBrush>

        <!-- Disabled State Colors -->
        <SolidColorBrush x:Key="ButtonDisabledBackground">Transparent</SolidColorBrush>
        <SolidColorBrush x:Key="ButtonDisabledForeground">#6D6D6D</SolidColorBrush>
        <SolidColorBrush x:Key="ButtonDisabledBorder">Transparent</SolidColorBrush>

        <!-- アニメーション用のトランジション時間 -->
        <x:Double x:Key="ButtonTransitionDuration">0.2</x:Double>
    </Styles.Resources>

    <!-- ベースボタンスタイル -->
    <!-- 🔥 [ISSUE#164_FIX] menu-itemスタイルと統合、通常状態は完全透明背景 -->
    <Style Selector="Button.translation-mode-button">
        <Setter Property="Background" Value="{StaticResource ButtonDefaultBackground}" />
        <Setter Property="Foreground" Value="{StaticResource ButtonDefaultForeground}" />
        <Setter Property="BorderBrush" Value="{StaticResource ButtonDefaultBorder}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="8" />
        <Setter Property="Margin" Value="4,2" />
        <Setter Property="Width" Value="50" />
        <Setter Property="CornerRadius" Value="6" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="Transitions">
            <Transitions>
                <TransformOperationsTransition Property="RenderTransform" Duration="0:0:0.2" />
                <BrushTransition Property="Background" Duration="0:0:0.2" />
                <BrushTransition Property="BorderBrush" Duration="0:0:0.2" />
            </Transitions>
        </Setter>
    </Style>

    <!-- Hover状態 -->
    <Style Selector="Button.translation-mode-button:pointerover">
        <Setter Property="Background" Value="{StaticResource ButtonHoverBackground}" />
        <Setter Property="BorderBrush" Value="{StaticResource ButtonHoverBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="RenderTransform" Value="scale(1.05)" />
    </Style>

    <!-- Pressed状態（クリック時） -->
    <Style Selector="Button.translation-mode-button:pressed">
        <Setter Property="RenderTransform" Value="scale(0.95)" />
    </Style>

    <!-- Pulsing状態（モードがアクティブな時のパルスアニメーション） -->
    <!-- 🔥 [ISSUE#164] アクティブ時は赤色アイコンのみ表示（背景・ボーダー変更なし） -->
    <!-- 🔥 [GEMINI_FIX] 'active'クラス名が:active疑似クラスと競合するため'pulsing'に変更 -->
    <!-- 🔥 [GEMINI_FIX2] ボタン全体ではなくImage要素のみにアニメーションを適用 -->
    <Style Selector="Button.translation-mode-button.pulsing Image">
        <!-- アクティブ時は背景やボーダーを変更せず、アイコンの色のみ変更 -->
        <!-- アイコン切り替えはViewModelのLiveIconSource/SingleshotIconSourceで実装済み -->

        <!-- 🔥 [ISSUE#164] 実行中の点滅効果（パルスアニメーション） -->
        <!-- 🔥 [ISSUE#164_TUNE] 速度を1.5秒→0.8秒、透過を0.6→0.2に変更（視認性向上） -->
        <Style.Animations>
            <Animation Duration="0:0:0.8" IterationCount="Infinite">
                <KeyFrame Cue="0%">
                    <Setter Property="Opacity" Value="1.0" />
                </KeyFrame>
                <KeyFrame Cue="50%">
                    <Setter Property="Opacity" Value="0.2" />
                </KeyFrame>
                <KeyFrame Cue="100%">
                    <Setter Property="Opacity" Value="1.0" />
                </KeyFrame>
            </Animation>
        </Style.Animations>
    </Style>

    <!-- Disabled状態 -->
    <Style Selector="Button.translation-mode-button:disabled">
        <Setter Property="Background" Value="{StaticResource ButtonDisabledBackground}" />
        <Setter Property="Foreground" Value="{StaticResource ButtonDisabledForeground}" />
        <Setter Property="BorderBrush" Value="{StaticResource ButtonDisabledBorder}" />
        <Setter Property="Opacity" Value="0.5" />
        <Setter Property="Cursor" Value="Arrow" />
    </Style>

    <!-- キーボードフォーカス時 -->
    <Style Selector="Button.translation-mode-button:focus-visible">
        <Setter Property="BorderBrush" Value="{StaticResource ButtonActiveBackground}" />
        <Setter Property="BorderThickness" Value="2" />
    </Style>

</Styles>
