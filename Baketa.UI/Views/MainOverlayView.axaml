<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="using:Baketa.UI.ViewModels"
        xmlns:local="using:Baketa.UI.Extensions"
        mc:Ignorable="d" d:DesignWidth="110" d:DesignHeight="600"
        x:Class="Baketa.UI.Views.MainOverlayView"
        x:DataType="vm:MainOverlayViewModel"
        Title="Baketa Overlay"
        SizeToContent="WidthAndHeight"
        MaxWidth="200"
        WindowStartupLocation="Manual"
        ShowInTaskbar="False"
        Topmost="True"
        Background="Transparent"
        CanResize="False"
        SystemDecorations="None"
        ExtendClientAreaToDecorationsHint="True"
        ExtendClientAreaChromeHints="NoChrome"
        ExtendClientAreaTitleBarHeightHint="0">
  
  <Window.Resources>
    <!-- カラーリソース -->
    <SolidColorBrush x:Key="OverlayBackgroundBrush" Color="#F5FFFFFF"/>
    <SolidColorBrush x:Key="OverlayBorderBrush" Color="#A0A0A0A0"/>
    <SolidColorBrush x:Key="ButtonBackgroundBrush" Color="Transparent"/>
    <SolidColorBrush x:Key="ButtonHoverBrush" Color="#F0F4F8"/>
    <SolidColorBrush x:Key="ButtonPressedBrush" Color="#E0E8F0"/>
    <SolidColorBrush x:Key="IconBrush" Color="#202020"/>
    <SolidColorBrush x:Key="IconDisabledBrush" Color="#C0C0C0"/>
    <SolidColorBrush x:Key="TextBrush" Color="#000000"/>
    <SolidColorBrush x:Key="StatusIndicatorBrush" Color="#17A2B8"/>
    
    <!-- ステータス別カラー -->
    <SolidColorBrush x:Key="StatusIdleBrush" Color="#6C757D"/>
    <SolidColorBrush x:Key="StatusActiveBrush" Color="#28A745"/>
    <SolidColorBrush x:Key="StatusProcessingBrush" Color="#FD7E14"/>
    <SolidColorBrush x:Key="StatusErrorBrush" Color="#DC3545"/>
  </Window.Resources>

  <Window.Styles>
    <!-- メニュー項目スタイル -->
    <!-- 🔥 [ISSUE#171] 多言語対応: Width固定値 → MinWidth に変更（テキスト長に応じて自動調整） -->
    <Style Selector="Button.menu-item">
      <Setter Property="Background" Value="{StaticResource ButtonBackgroundBrush}"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Padding" Value="8"/>
      <Setter Property="HorizontalContentAlignment" Value="Center"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
      <Setter Property="HorizontalAlignment" Value="Stretch"/>
      <Setter Property="CornerRadius" Value="6"/>
      <Setter Property="Margin" Value="4,2"/>
      <Setter Property="MinWidth" Value="50"/>
    </Style>
    
    <Style Selector="Button.menu-item:hover">
      <Setter Property="Background" Value="{StaticResource ButtonHoverBrush}"/>
    </Style>
    
    <Style Selector="Button.menu-item:pressed">
      <Setter Property="Background" Value="{StaticResource ButtonPressedBrush}"/>
    </Style>
    
    <!-- ステータス表示スタイル（未使用 - Status削除済み） -->
    <Style Selector="Border.status-item">
      <Setter Property="Background" Value="{StaticResource ButtonBackgroundBrush}"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Padding" Value="8"/>
      <Setter Property="HorizontalAlignment" Value="Stretch"/>
      <Setter Property="CornerRadius" Value="6"/>
      <Setter Property="Margin" Value="4,2"/>
      <Setter Property="MinWidth" Value="50"/>
    </Style>
    
    <!-- 最小化ボタンスタイル -->
    <!-- 🔥 [ISSUE#171] 多言語対応: Width固定値 → MinWidth に変更 -->
    <Style Selector="Button.minimize-button">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Padding" Value="4"/>
      <Setter Property="HorizontalContentAlignment" Value="Center"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
      <Setter Property="CornerRadius" Value="4"/>
      <Setter Property="HorizontalAlignment" Value="Stretch"/>
      <Setter Property="MinWidth" Value="50"/>
      <Setter Property="Margin" Value="4,8,4,4"/>
    </Style>
    
    <Style Selector="Button.minimize-button:hover">
      <Setter Property="Background" Value="{StaticResource ButtonHoverBrush}"/>
    </Style>
    
    <!-- 非アクティブボタンスタイル - 背景色なし、アイコンとテキストのみ薄いグレー -->
    <Style Selector="Button.menu-item:disabled">
      <Setter Property="Background" Value="Transparent"/>
    </Style>
    
    <Style Selector="Button.menu-item:disabled PathIcon">
      <Setter Property="Foreground" Value="{StaticResource IconDisabledBrush}"/>
    </Style>
    
    <Style Selector="Button.menu-item:disabled TextBlock">
      <Setter Property="Foreground" Value="{StaticResource IconDisabledBrush}"/>
    </Style>
    
    <!-- 非アクティブ状態でホバー効果も無効化 -->
    <Style Selector="Button.menu-item:disabled:hover">
      <Setter Property="Background" Value="Transparent"/>
    </Style>

    <!-- ドラッグハンドルスタイル (#246) -->
    <Style Selector="Border.drag-handle">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="Cursor" Value="SizeAll"/>
      <Setter Property="HorizontalAlignment" Value="Stretch"/>
      <Setter Property="Padding" Value="4"/>
    </Style>

    <Style Selector="Border.drag-handle:pointerover">
      <Setter Property="Background" Value="{StaticResource ButtonHoverBrush}"/>
    </Style>
  </Window.Styles>

  <!-- メインコンテナ -->
  <Grid>
  
  <!-- メインオーバーレイ: 縦型アイコン+テキスト構成 -->
  <Border Name="MainOverlayBorder"
          Background="{StaticResource OverlayBackgroundBrush}"
          BorderBrush="{StaticResource OverlayBorderBrush}"
          BorderThickness="1"
          CornerRadius="8"
          BoxShadow="0 4 12 0 #40000000"
          Padding="4"
          IsVisible="{Binding !IsCollapsed}">

      <StackPanel Orientation="Vertical" Spacing="2">

      <!-- ドラッグハンドル (#246) -->
      <Border Classes="drag-handle"
              Name="DragHandle"
              CornerRadius="4"
              Margin="4,4,4,2"
              Height="20"
              ToolTip.Tip="{local:Localize MainOverlay_DragToMove}"
              PointerPressed="OnDragHandlePointerPressed">
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
          <!-- 6ドットグリップパターン (⋮⋮) -->
          <StackPanel Orientation="Horizontal" Spacing="3">
            <StackPanel Orientation="Vertical" Spacing="2">
              <Ellipse Width="3" Height="3" Fill="{StaticResource IconBrush}" Opacity="0.5"/>
              <Ellipse Width="3" Height="3" Fill="{StaticResource IconBrush}" Opacity="0.5"/>
              <Ellipse Width="3" Height="3" Fill="{StaticResource IconBrush}" Opacity="0.5"/>
            </StackPanel>
            <StackPanel Orientation="Vertical" Spacing="2">
              <Ellipse Width="3" Height="3" Fill="{StaticResource IconBrush}" Opacity="0.5"/>
              <Ellipse Width="3" Height="3" Fill="{StaticResource IconBrush}" Opacity="0.5"/>
              <Ellipse Width="3" Height="3" Fill="{StaticResource IconBrush}" Opacity="0.5"/>
            </StackPanel>
          </StackPanel>
        </Grid>
      </Border>

      <!-- 最小化ボタン - ウィンドウ選択後のみ表示 -->
      <!-- 🔥 [ISSUE#171] UI改善: Minテキスト削除、アイコンサイズをexpandと統一（16px） -->
      <Button Classes="minimize-button"
              Command="{Binding FoldCommand}"
              ToolTip.Tip="{local:Localize MainOverlay_Minimize}"
              AutomationProperties.Name="最小化ボタン"
              IsVisible="{Binding IsWindowSelected}">
        <Image Width="16" Height="16"
               Source="avares://Baketa/Assets/Icons/minimize.png"
               VerticalAlignment="Center"/>
      </Button>
      
      <!-- ウィンドウ選択ボタン - 常に表示 -->
      <!-- 🔥 [ISSUE#171] UI改善: Select → Target に変更 -->
      <Button Classes="menu-item"
              Command="{Binding SelectWindowCommand}"
              ToolTip.Tip="{local:Localize MainOverlay_SelectWindow}"
              Name="SelectWindowButton"
              IsEnabled="{Binding IsSelectWindowEnabled}"
              AutomationProperties.Name="翻訳対象ウィンドウを選択">
        <StackPanel Orientation="Vertical" Spacing="4">
          <Image Width="18" Height="18"
                 Source="avares://Baketa/Assets/Icons/target.png"/>
          <TextBlock Text="{local:Localize MainOverlay_Target}"
                     FontSize="10"
                     HorizontalAlignment="Center"
                     TextAlignment="Center"
                     Foreground="{StaticResource TextBrush}"
                     IsVisible="{Binding !IsCollapsed}"/>
        </StackPanel>
      </Button>
      
      <!-- Live翻訳ボタン - ウィンドウ選択後のみ表示 -->
      <!-- 🔥 [ISSUE#171] UI改善: Start → Live翻訳 に変更、状態による表示制御 -->
      <Button Classes="translation-mode-button"
              Classes.pulsing="{Binding IsLiveActive}"
              IsEnabled="{Binding IsLiveEnabled}"
              IsVisible="{Binding IsWindowSelected}"
              Command="{Binding StartStopCommand}"
              ToolTip.Tip="{Binding StartButtonTooltip}"
              Name="StartStopButton"
              PointerPressed="StartStopButton_OnPointerPressed">
        <StackPanel Orientation="Vertical" Spacing="4">
          <!-- 🔥 [ISSUE#164] ObservableAsPropertyHelperでIImage型を直接バインディング（ReactiveUIベストプラクティス） -->
          <Image Width="20" Height="20"
                 Source="{Binding LiveIconSource}" />
          <TextBlock Text="{local:Localize MainOverlay_LiveTranslation}"
                     FontSize="10"
                     HorizontalAlignment="Center"
                     TextAlignment="Center"
                     Foreground="{StaticResource TextBrush}"
                     IsVisible="{Binding !IsCollapsed}"/>
        </StackPanel>
      </Button>

      <!-- Shot翻訳ボタン - ウィンドウ選択後のみ表示 -->
      <!-- 🔥 [ISSUE#171] UI改善: Shot → Shot翻訳 に変更、状態による表示制御 -->
      <Button Classes="translation-mode-button"
              Classes.pulsing="{Binding IsSingleshotActive}"
              IsEnabled="{Binding IsSingleshotEnabled}"
              IsVisible="{Binding IsWindowSelected}"
              Command="{Binding ExecuteSingleshotCommand}"
              ToolTip.Tip="{Binding SingleshotButtonTooltip}"
              Name="SingleshotButton">
        <StackPanel Orientation="Vertical" Spacing="4">
          <!-- 🔥 [ISSUE#164] ObservableAsPropertyHelperでIImage型を直接バインディング（ReactiveUIベストプラクティス） -->
          <Image Width="20" Height="20"
                 Source="{Binding SingleshotIconSource}" />
          <TextBlock Text="{local:Localize MainOverlay_ShotTranslation}"
                     FontSize="10"
                     HorizontalAlignment="Center"
                     TextAlignment="Center"
                     Foreground="{StaticResource TextBrush}"
                     IsVisible="{Binding !IsCollapsed}"/>
        </StackPanel>
      </Button>

      <!-- ローディングインジケーター -->
      <Border Classes="status-item" IsVisible="{Binding IsLoading}">
        <StackPanel Orientation="Vertical" Spacing="4">
          <Ellipse Width="16" Height="16"
                   Fill="{StaticResource StatusProcessingBrush}"
                   Opacity="0.8"/>
          <TextBlock Text="{local:Localize MainOverlay_Preparing}"
                     FontSize="9"
                     HorizontalAlignment="Center"
                     Foreground="{StaticResource StatusProcessingBrush}"/>
        </StackPanel>
      </Border>
      
      <!-- 翻訳表示切り替えボタン - ウィンドウ選択後のみ表示 -->
      <!-- 🔥 [ISSUE#171] UI改善: Show → 翻訳表示 に変更、状態による表示制御 -->
      <Button Classes="menu-item"
              Command="{Binding ShowHideCommand}"
              IsEnabled="{Binding ShowHideEnabled}"
              IsVisible="{Binding IsWindowSelected}"
              ToolTip.Tip="{local:Localize MainOverlay_ShowHideTranslation}">
        <StackPanel Orientation="Vertical" Spacing="4" HorizontalAlignment="Center">
          <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Image Width="18" Height="18"
                   Source="avares://Baketa/Assets/Icons/visible.png"/>
            <Ellipse Width="8" Height="8"
                     Fill="{StaticResource StatusActiveBrush}"
                     Margin="2,0,0,0"
                     IsVisible="{Binding IsTranslationActive}"/>
          </StackPanel>
          <TextBlock Text="{local:Localize MainOverlay_ShowTranslation}"
                     FontSize="10"
                     HorizontalAlignment="Center"
                     Foreground="{StaticResource TextBrush}"
                     IsVisible="{Binding !IsCollapsed}"/>
        </StackPanel>
      </Button>
      
      <!-- 設定ボタン - 常に表示 -->
      <!-- 🔥 [ISSUE#171] UI改善: Set → 設定 に変更 -->
      <Button Classes="menu-item"
              Command="{Binding SettingsCommand}"
              IsEnabled="{Binding SettingsEnabled}"
              ToolTip.Tip="{local:Localize MainOverlay_Settings}">
        <StackPanel Orientation="Vertical" Spacing="4">
          <Image Width="18" Height="18"
                 Source="avares://Baketa/Assets/Icons/settings.png"/>
          <TextBlock Text="{local:Localize MainOverlay_Settings}"
                     FontSize="10"
                     HorizontalAlignment="Center"
                     Foreground="{StaticResource TextBrush}"
                     IsVisible="{Binding !IsCollapsed}"/>
        </StackPanel>
      </Button>
      
      <!-- 区切り線 -->
      <Rectangle Height="1"
                 Fill="{StaticResource OverlayBorderBrush}"
                 Margin="8,4"
                 Opacity="0.5"/>

      <!-- 終了ボタン - 常に表示 -->
      <!-- 🔥 [ISSUE#171] UI改善: Exit テキストは展開時のみ表示 -->
      <Button Classes="menu-item"
              Command="{Binding ExitCommand}"
              ToolTip.Tip="{local:Localize MainOverlay_Exit}"
              Click="OnExitButtonClick">
        <StackPanel Orientation="Vertical" Spacing="4">
          <Image Width="18" Height="18"
                 Source="avares://Baketa/Assets/Icons/exit.png"/>
          <TextBlock Text="{local:Localize MainOverlay_Exit}"
                     FontSize="10"
                     HorizontalAlignment="Center"
                     Foreground="{StaticResource TextBrush}"
                     IsVisible="{Binding !IsCollapsed}"/>
        </StackPanel>
      </Button>

    </StackPanel>
  </Border>

  <!-- 最小化状態 -->
  <!-- 🔥 [ISSUE#171] UI改善: Live翻訳、Shot翻訳、翻訳表示ボタン追加（アイコンのみ） -->
  <!-- Height削除: ウィンドウ選択状態に応じて自動調整 -->
  <Border Name="CollapsedOverlayBorder"
          Width="40"
          Background="{StaticResource OverlayBackgroundBrush}"
          BorderBrush="{StaticResource OverlayBorderBrush}"
          BorderThickness="1"
          CornerRadius="8"
          BoxShadow="0 2 8 0 #40000000"
          IsVisible="{Binding IsCollapsed}">

    <StackPanel Orientation="Vertical" Spacing="0">
      <!-- ドラッグハンドル（縮小時） (#246) -->
      <Border Classes="drag-handle"
              Name="CollapsedDragHandle"
              CornerRadius="4"
              Margin="2,2,2,0"
              Height="16"
              ToolTip.Tip="{local:Localize MainOverlay_DragToMove}"
              PointerPressed="OnDragHandlePointerPressed">
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
          <!-- 6ドットグリップパターン (⋮⋮) - 縮小版 -->
          <StackPanel Orientation="Horizontal" Spacing="2">
            <StackPanel Orientation="Vertical" Spacing="1">
              <Ellipse Width="2" Height="2" Fill="{StaticResource IconBrush}" Opacity="0.5"/>
              <Ellipse Width="2" Height="2" Fill="{StaticResource IconBrush}" Opacity="0.5"/>
              <Ellipse Width="2" Height="2" Fill="{StaticResource IconBrush}" Opacity="0.5"/>
            </StackPanel>
            <StackPanel Orientation="Vertical" Spacing="1">
              <Ellipse Width="2" Height="2" Fill="{StaticResource IconBrush}" Opacity="0.5"/>
              <Ellipse Width="2" Height="2" Fill="{StaticResource IconBrush}" Opacity="0.5"/>
              <Ellipse Width="2" Height="2" Fill="{StaticResource IconBrush}" Opacity="0.5"/>
            </StackPanel>
          </StackPanel>
        </Grid>
      </Border>

      <!-- 展開ボタン -->
      <Button Classes="minimize-button"
              Command="{Binding FoldCommand}"
              ToolTip.Tip="{local:Localize MainOverlay_Expand}"
              Background="Transparent"
              BorderThickness="0"
              Width="36"
              Height="36"
              Margin="2">
        <Image Width="16" Height="16"
               Source="avares://Baketa/Assets/Icons/expand.png"/>
      </Button>

      <!-- Live翻訳ボタン（縮小時） - ウィンドウ選択後のみ表示 -->
      <Button Classes="minimize-button"
              Command="{Binding StartStopCommand}"
              ToolTip.Tip="{Binding StartButtonTooltip}"
              IsEnabled="{Binding IsLiveEnabled}"
              IsVisible="{Binding IsWindowSelected}"
              Background="Transparent"
              BorderThickness="0"
              Width="36"
              Height="36"
              Margin="2"
              PointerPressed="StartStopButton_OnPointerPressed">
        <Image Width="16" Height="16"
               Source="{Binding LiveIconSource}"/>
      </Button>

      <!-- Shot翻訳ボタン（縮小時） - ウィンドウ選択後のみ表示 -->
      <Button Classes="minimize-button"
              Command="{Binding ExecuteSingleshotCommand}"
              ToolTip.Tip="{Binding SingleshotButtonTooltip}"
              IsEnabled="{Binding IsSingleshotEnabled}"
              IsVisible="{Binding IsWindowSelected}"
              Background="Transparent"
              BorderThickness="0"
              Width="36"
              Height="36"
              Margin="2">
        <Image Width="16" Height="16"
               Source="{Binding SingleshotIconSource}"/>
      </Button>

      <!-- 翻訳表示ボタン（縮小時） - ウィンドウ選択後のみ表示 -->
      <Button Classes="minimize-button"
              Command="{Binding ShowHideCommand}"
              ToolTip.Tip="{local:Localize MainOverlay_ShowHideTranslation}"
              IsEnabled="{Binding ShowHideEnabled}"
              IsVisible="{Binding IsWindowSelected}"
              Background="Transparent"
              BorderThickness="0"
              Width="36"
              Height="36"
              Margin="2">
        <StackPanel>
          <Image Width="16" Height="16"
                 Source="avares://Baketa/Assets/Icons/visible.png"/>
          <Ellipse Width="6" Height="6"
                   Fill="{StaticResource StatusActiveBrush}"
                   Margin="0,-2,0,0"
                   HorizontalAlignment="Center"
                   IsVisible="{Binding IsTranslationActive}"/>
        </StackPanel>
      </Button>
    </StackPanel>
  </Border>

  </Grid>
</Window>