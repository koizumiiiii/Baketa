# Baketa gRPC Translation Server Dependencies
# Phase 2.2: Python gRPC Server

# gRPC (pinned versions for stability)
# [Issue #212] protobuf 5.x fixed for PyInstaller compatibility
# - grpcio-tools < 1.70.0 required for Protobuf 5.x code generation
# - Exact version pinning prevents gencode/runtime version mismatch
grpcio>=1.60.0
grpcio-tools==1.68.1
protobuf==5.29.3

# [Issue #185] tokenizers library (Rust-based, lightweight)
# Removed transformers/torch, load tokenizer.json directly
# Effect: Package size reduction ~450MB (torch ~300MB + transformers ~150MB)
tokenizers>=0.15.0

# Data Processing
numpy>=1.24.0

# System Monitoring
psutil>=5.9.0

# [PHASE1.1] GPU/VRAM monitoring (NVIDIA GPU)
pynvml>=11.5.0

# Optional: GPU support (uncomment if CUDA is available)
# torch[cuda]>=2.0.0

# Phase 2.2.1: CTranslate2 for optimized inference (80% memory reduction: 2.4GB -> 500MB)
ctranslate2>=3.20.0

# [ALPHA_0.1.2] HuggingFace Hub integration for automatic model download
huggingface_hub>=0.20.0

# Development & Testing
pytest>=7.0.0
pytest-asyncio>=0.23.0
